const header = {
  sm: '7rem',
  md: '8rem'
}

// const label = ['sm', 'md', 'lg', 'xl']

const padding = {
  sm: '2rem'
}

const margin = {
  dividerSm: '4rem',
  dividerMd: '6rem',
  sm: '1.5rem'
}

export default { header, margin, padding }
